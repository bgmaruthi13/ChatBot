Django==6.0.7
djangorestframework==3.17.1
python-dotenv==1.2.2

# PDF extraction (Epic 2) - digital-text PDFs only, no OCR (see
# BACKLOG.md 2.2: dropped, needs system binaries this env can't install)
pypdf==6.14.2

# Embedding + vector storage (Epic 4, 5)
sentence-transformers==5.6.0
chromadb==1.5.9
numpy==2.5.1

# LLM providers (Epic 9) - inactive unless LLM_PROVIDER + the matching
# key is set; ExtractiveProvider (Epic 7) is the default with none.
anthropic==0.117.0
openai==2.46.0
requests==2.34.2
